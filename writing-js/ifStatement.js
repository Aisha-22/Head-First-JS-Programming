// Create a variable named price and assign the value 28.99 to it.
var price = 28.99;

// Create a variable named discount and assign the value 10 to it.
var discount = 10;

// Compute a new price by applying discount and the assign it to the variable total. 
var total = price - (price * (discount/100));

// Compare the value in the variable total to 26. 
if(total > 26) {
// It the variable total is greater than 26, do something with freeShipping
    // freeShipping();
    console.log("Free Shipping");
// End the 'if statement.
} 