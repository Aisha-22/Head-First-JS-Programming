// Create the variable, named circleRadius, and assign the value 20 to it.
var circleRadius = 20;

// Create the variable, named circleArea, and assign the result of this expression to it (1256.6370614359173).
var circleArea = Math.PI * (circleRadius * circleRadius);